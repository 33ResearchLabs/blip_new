/**
 * Compatibility shim — the real component now lives in the
 * `@/plugins/issue-reporter` folder. Old imports keep working.
 */

export { RegionSelector, type Region } from '@/plugins/issue-reporter/RegionSelector';
